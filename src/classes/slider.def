#ifndef TRCL_SLIDER_DEFINED
#define TRCL_SLIDER_DEFINED

  #include "displayobject.def"

  struct TROD_Slider
  {
    struct TROD_DisplayObject DO;
    WORD                      Min;
    WORD                      Max;
    ULONG                     Flags;
    ULONG                     Value;
  };

  extern TR_METHOD(Slider,NEW,NewData);
  extern TR_METHOD(Slider,INSTALL,InstallData);
  extern TR_METHOD(Slider,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(Slider,GETATTRIBUTE);
  extern TR_METHOD(Slider,KEYDOWN_REPEATEDKEYDOWN,EventData);
  extern TR_SIMPLEMETHOD(Slider,DISABLED_ENABLED);
  extern TR_METHOD(Slider,EVENT,EventData);

#endif
