#ifndef TRCL_SCROLLER_DEFINED
#define TRCL_SCROLLER_DEFINED

  #include "displayobject.def"

  struct TROD_Scroller
  {
    struct TROD_DisplayObject DO;
    WORD                      Total;
    WORD                      Visible;
    ULONG                     Flags;
    ULONG                     Value;
  };

  extern TR_METHOD(Scroller,NEW,NewData);
  extern TR_METHOD(Scroller,INSTALL,InstallData);
  extern TR_METHOD(Scroller,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(Scroller,GETATTRIBUTE);
  extern TR_METHOD(Scroller,KEYDOWN_REPEATEDKEYDOWN,EventData);
  extern TR_SIMPLEMETHOD(Scroller,DISABLED_ENABLED);
  extern TR_METHOD(Scroller,EVENT,EventData);

#endif
