#ifndef TRCL_CYCLE_DEFINED
#define TRCL_CYCLE_DEFINED

  #include "displayobject.def"

  struct TROD_Cycle
  {
    struct TROD_DisplayObject DO;
    STRPTR *                  Entries;
    ULONG                     Number;
    ULONG                     Flags;
    ULONG                     TextWidth;
    ULONG                     Spacing;
    ULONG                     GT_Kind;
    ULONG                     GT_Labels;
    ULONG                     GT_Active;
    ULONG                     GT_Spacing;
    ULONG                     Value;
  };

  extern TR_METHOD(Cycle,NEW,NewData);
  extern TR_METHOD(Cycle,INSTALL,InstallData);
  extern TR_METHOD(Cycle,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(Cycle,GETATTRIBUTE);
  extern TR_SIMPLEMETHOD(Cycle,DISABLED_ENABLED);
  extern TR_METHOD(Cycle,KEYDOWN_REPEATEDKEYDOWN,EventData);
  extern TR_METHOD(Cycle,EVENT,EventData);

#endif
