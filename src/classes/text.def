#ifndef TRCL_TEXT_DEFINED
#define TRCL_TEXT_DEFINED

  #include "displayobject.def"

  struct TROD_Text
  {
    struct TROD_DisplayObject DO;
    ULONG                     TextLength; // Length of the string in bytes
  };

  extern TR_METHOD(Text,NEW,NewData);
  extern TR_SIMPLEMETHOD(Text,INSTALL_REFRESH);
  extern TR_METHOD(Text,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(Text,DISPOSE);

#endif
