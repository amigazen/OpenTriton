#ifndef TRCL_IMAGE_DEFINED
#define TRCL_IMAGE_DEFINED

  #include "displayobject.def"

  struct TROD_Image
  {
    struct TROD_DisplayObject DO;
    struct Image *            Image;
    struct IClass *           IClass;
  };

  extern TR_METHOD(Image,NEW,NewData);
  extern TR_SIMPLEMETHOD(Image,REFRESH);
  extern TR_METHOD(Image,INSTALL,InstallData);
  extern TR_METHOD(Image,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(Image,REMOVE);

#endif
