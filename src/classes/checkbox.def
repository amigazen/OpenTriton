#ifndef TRCL_CHECKBOX_DEFINED
#define TRCL_CHECKBOX_DEFINED

  #include "displayobject.def"

  struct TROD_CheckBox
  {
    struct TROD_DisplayObject DO;
    BOOL                      Checked;
  };

  extern TR_METHOD(CheckBox,NEW,NewData);
  extern TR_SIMPLEMETHOD(CheckBox,INSTALL);
  extern TR_METHOD(CheckBox,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(CheckBox,GETATTRIBUTE);
  extern TR_METHOD(CheckBox,KEYDOWN_DISABLED_ENABLED,EventData);
  extern TR_METHOD(CheckBox,EVENT,EventData);

#endif
