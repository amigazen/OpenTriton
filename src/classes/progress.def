#ifndef TRCL_PROGRESS_DEFINED
#define TRCL_PROGRESS_DEFINED

  #include "displayobject.def"

  struct TROD_Progress
  {
    struct TROD_DisplayObject DO;
    ULONG                     Value;
  };

  extern TR_METHOD(Progress,NEW,NewData);
  extern TR_SIMPLEMETHOD(Progress,INSTALL_REFRESH);
  extern TR_METHOD(Progress,SETATTRIBUTE,SetAttributeData);
  extern TR_SIMPLEMETHOD(Progress,GETATTRIBUTE);

#endif
